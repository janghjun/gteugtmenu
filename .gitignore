# 의존성
node_modules/

# 빌드 결과물 (Vercel이 직접 빌드하므로 커밋 불필요)
dist/

# AIT 번들 — 콘솔 업로드용, git 추적 불필요
*.ait

# 환경변수
.env
.env.local
.env.*.local

# 에디터 / OS
.DS_Store
*.local
.vscode/settings.json

# Claude Code — 개인 설정만 제외, 프로젝트 툴링은 추적
.claude/settings.local.json
.claude/cache/
.claude/tmp/

# 콘텐츠 드래프트 — 활성화 전까지 미추적
src/features/content/*.draft.json
