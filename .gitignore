# 의존성
node_modules/

# 빌드 결과물 (Vercel이 직접 빌드하므로 커밋 불필요)
dist/

# 환경변수
.env
.env.local
.env.*.local

# 에디터 / OS
.DS_Store
*.local
.vscode/settings.json

# Claude Code — 개인 설정만 제외, 프로젝트 툴링은 추적
.claude/settings.local.json
.claude/cache/
.claude/tmp/
